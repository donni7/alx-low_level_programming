#include "main.h"
/**
 * _abs - Check Main
 * @r: An integer
 * Description: returns absolute value of an input
 * Return: Absolute value of a number r
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
